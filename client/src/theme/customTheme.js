import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    mode: "dark",
    background: {
      paper: "#151515",
      default: "rgba(0,0,0,.96)",
    },
  },
});
